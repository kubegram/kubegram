{
  "name": "@kubegram/kubegram-auth",
  "version": "0.1.0",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "description": "OAuth authentication library built on OpenAuth.js with multiple provider support",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "bun run clean",
    "prepublishOnly": "bun run build",
    "lint": "eslint src/**/*.ts --ignore-pattern '**/__tests__/**' --ignore-pattern '**/*.test.ts' --no-warn-ignored",
    "lint:fix": "eslint src/**/*.ts --ignore-pattern '**/__tests__/**' --ignore-pattern '**/*.test.ts' --no-warn-ignored --fix",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check src/**/*.ts",
    "type-check": "tsc --noEmit",
    "check-all": "bun run type-check && bun run lint && bun run format:check"
  },
  "keywords": [
    "oauth",
    "authentication",
    "auth",
    "openauth",
    "oauth2",
    "typescript"
  ],
  "author": "Kubegram",
  "license": "BUSL-1.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/kubegram/kubegram.git",
    "directory": "kubegram-auth"
  },
  "bugs": {
    "url": "https://github.com/kubegram/kubegram/issues"
  },
  "homepage": "https://github.com/kubegram/kubegram/tree/main/kubegram-auth#readme",
  "devDependencies": {
    "@types/node": "^24.10.13",
    "hono": "^4.11.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@kubegram/common-events": "^1.1.0",
    "@openauthjs/openauth": "^0.4.3",
    "valibot": "^1.2.0"
  },
  "peerDependencies": {
    "ioredis": "^5.4.0",
    "lru-cache": "^11.0.0",
    "redis": "^4.7.1"
  },
  "peerDependenciesMeta": {
    "ioredis": {
      "optional": true
    }
  }
}
