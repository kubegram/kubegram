name: PR Validation - common-ts

on:
  pull_request:
    branches:
      - main
    paths:
      - 'common-ts/**'

jobs:
  validate:
    runs-on: ubuntu-latest


    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: make ci-install

      - name: Type check
        run: make ci-typecheck-common

      - name: Lint code
        run: make ci-lint-common

      - name: Run tests
        run: make ci-test-common

      - name: Build
        run: make ci-build-common

      - name: Verify build output
        working-directory: common-ts
        run: |
          if [ ! -d "dist" ]; then
            echo "Build failed - dist directory not found"
            exit 1
          fi
          echo "Build successful - dist directory created"

      - name: Generate GraphQL types
        run: make ci-codegen-common

      - name: Verify GraphQL generation
        working-directory: common-ts
        run: |
          if [ ! -f "src/graphql/generated/types.ts" ]; then
            echo "GraphQL codegen failed - types.ts not found"
            exit 1
          fi
          echo "GraphQL codegen successful"

      - name: Run tests after GraphQL generation
        run: make ci-test-common
