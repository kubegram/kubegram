import React from 'react';

/**
 * CanvasBackground Component
 *
 * Renders a clean background without dots for better performance.
 * The dots were removed to prevent lag across the application.
 */
const CanvasBackground: React.FC = () => {
  // Return empty fragment - no background dots for better performance
  return <></>;
};

export default CanvasBackground;
