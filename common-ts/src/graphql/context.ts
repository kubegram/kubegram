/**
 * GraphQL Context
 * This file defines the context type for GraphQL resolvers
 */

export interface GraphQLContext {
  // Add any context properties you need
  // For example: user, database connection, etc.
}
